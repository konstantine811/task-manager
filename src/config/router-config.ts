/** Route path constants for task-manager (app base path is /app). */
export const RoutPath = {
  TASK_MANAGER: "/app",
  TASK_MANAGER_DAILY: "daily/:id",
  TASK_MANAGER_TEMPLATE: "template",
  TASK_MANAGER_ANALYTICS: "analytics",
} as const;
