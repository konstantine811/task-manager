export enum SoundTypeElement {
  BUTTON = "button",
  SELECT = "select",
  SELECT_2 = "select-2",
  LINK = "link",
  LOGO = "logo",
  NONE = "none",
  SHIFT = "shift",
  OPEN = "open",
}

export enum HoverStyleElement {
  circle = "circle",
  quad = "quad",
  none = "none",
}
